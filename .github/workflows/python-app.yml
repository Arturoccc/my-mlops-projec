name: Python CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-22.04  # Change to Ubuntu 22.04

    strategy:
      matrix:
        python-version: [3.8, 3.9, 3.10]  # Use stable Python versions

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Format code
      run: make format

    - name: Lint code
      run: make lint

    - name: Run tests
      run: make test
  